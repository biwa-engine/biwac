import math::pos::pos_new;

fn add(x: Int, y: Int) -> Int {
    return x + y;
}

fn main() {
  let x = add(3, 2);
  let y = math::fact(x);
  let l = math::line::Line{
    begin = pos_new(0, x),
    end = pos_new(11, y),
  };
}
